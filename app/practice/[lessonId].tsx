/**
 * Practice flow (reference audio, plan / paywall) lives in `PracticeScreen`.
 */
import PracticeScreen from '@/src/features/practice/screens/PracticeScreen';

export default function PracticeLessonRoute() {
  return <PracticeScreen />;
}
